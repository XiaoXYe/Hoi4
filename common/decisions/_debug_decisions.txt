debug_decisions = {
	
	debug_chicken_chicken_dinner = {
		icon = generic_research

		available = {
			is_debug = yes
		}

		visible = {
			always = yes
		}

		fire_only_once = no
		cost = 0

		complete_effect = {
			every_country = {
				limit = {
						NOT = {
							original_tag = TST
						}
				}
				every_other_country = {
					limit = {
						NOT = {
							OR = {
								original_tag = TST
								has_war_with = PREV
							}
						}
					}
					declare_war_on = {
						target = PREV
						type = annex_everything
					}
				}
			}
			every_state = {
				limit = {
					NOT = {
						is_core_of = TST
					}
				}
				every_country = {
					limit = {
						NOT = {
							original_tag = TST
						}
					}
					add_state_claim = PREV
				}
			}
		}
	}
}