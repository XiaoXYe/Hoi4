LIT_power_endgame_category = {
	LIT_power_endgame_mission = {
		activation = {
			has_country_flag = LIT_power_endgame_begin
		}
		available = {
			has_country_flag = LIT_power_endgmame_can_end_flag
		}
		cancel_trigger = {
			always = no
		}
		icon = mission_icon     # For GFX_decision_mission_icon
		is_good = no
		days_mission_timeout = 180
		selectable_mission = no
		complete_effect = {
			country_event = {
				id = litang.22
				days = 3
			}
			clr_country_flag = LIT_power_endgame_begin
			clr_country_flag = LIT_power_endgmame_can_end_flag
		}
		timeout_effect = {
			country_event = {
				id = litang.22001
				days = 3
			}
			clr_country_flag = LIT_power_endgame_begin
			clr_country_flag = LIT_power_endgmame_can_end_flag
		}
	}
}

LIT_balance_of_power_category = {
	LIT_bop_refuse_business_order = {
		priority = 100
		icon = GFX_decision_eng_trade_unions_demand

		allowed = {
			original_tag = LIT
		}

		cost = 30
		days_re_enable = 30

		ai_will_do = {
			base = 10
		}

		complete_effect = {
			add_power_balance_value = {
				id = LIT_power_balance
				value = 0.1
				tooltip_side = LIT_king_side
			}
		}
	}

	LIT_bop_accept_business_order = {
		priority = 98
		icon = GFX_decision_eng_trade_unions_demand

		allowed = {
			original_tag = LIT
		}

		cost = 0
		days_re_enable = 30

		ai_will_do = {
			base = 0
		}

		complete_effect = {
			add_power_balance_value = {
				id = LIT_power_balance
				value = -0.1
				tooltip_side = LIT_capital_side
			}
		}
	}

	LIT_bop_hire_trusted_followers = {
		priority = 97
		icon = GFX_decision_eng_trade_unions_demand

		allowed = {
			original_tag = LIT
		}

		cost = 50
		fire_only_once = yes
		ai_will_do = {
			base = 50
		}

		days_remove = 30
		remove_effect = {
			add_power_balance_value = {
				id = LIT_power_balance
				value = 0.2
				tooltip_side = LIT_king_side
			}
		}
	}

	LIT_bop_bribe_dingzhen_family = {
		priority = 95
		icon = GFX_decision_eng_trade_unions_demand

		allowed = {
			original_tag = LIT
		}

		cost = 50
		fire_only_once = yes
		ai_will_do = {
			base = 0
		}

		days_remove = 30
		complete_effect = {
			add_power_balance_value = {
				id = LIT_power_balance
				value = -0.2
				tooltip_side = LIT_capital_side
			}
		}
	}

}

LIT_litangyia_category = {
	LIT_litangyia_highway_build = {
		icon = generic_research

		visible = {
			has_completed_focus = LIT_litang_urbanization
		}

		available = {
		}

		fire_only_once = yes
		cost = 30
		days_remove = 30

		ai_will_do = {
			factor = 90
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision LIT_litangyia_highway_build"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove LIT_litangyia_highway_build"
			set_country_flag = LIT_litangyia_highway_build_flag
			188 = {
				add_building_construction = {
					type = infrastructure
					level = 1
					instant_build = yes
				}
			}
		}
	}
	LIT_litangyia_industrial_complex_build_1 = {
		icon = generic_research

		visible = {
			has_completed_focus = LIT_litang_urbanization
		}

		available = {
		}

		fire_only_once = yes
		cost = 30
		days_remove = 60

		ai_will_do = {
			factor = 90
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision LIT_litangyia_industrial_complex_build_1"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove LIT_litangyia_industrial_complex_build_1"
			set_country_flag = LIT_litangyia_industrial_complex_build_1_flag
			188 = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = industrial_complex
					level = 1
					instant_build = yes
				}
			}
		}
	}
	LIT_litangyia_arms_factory_build_1 = {
		icon = generic_research

		visible = {
			has_completed_focus = LIT_litang_urbanization
		}

		available = {
		}

		fire_only_once = yes
		cost = 30
		days_remove = 60

		ai_will_do = {
			factor = 90
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision LIT_litangyia_arms_factory_build_1"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove LIT_litangyia_arms_factory_build_1"
			set_country_flag = LIT_litangyia_arms_factory_build_1_flag
			188 = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = arms_factory
					level = 1
					instant_build = yes
				}
			}
		}
	}

	LIT_litangyia_construct_1 = {
		icon = generic_research

		visible = {
			has_completed_focus = LIT_litang_urbanization
		}

		available = {
			AND = {
				has_country_flag = LIT_litangyia_highway_build_flag
				has_country_flag = LIT_litangyia_industrial_complex_build_1_flag
				has_country_flag = LIT_litangyia_arms_factory_build_1_flag
			}
		}

		fire_only_once = yes
		cost = 40
		days_remove = 30

		ai_will_do = {
			factor = 90
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision LIT_litangyia_construct_1"

		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove LIT_litangyia_construct_1"
			set_country_flag = LIT_litangyia_construct_1_flag
			188 = {
				set_state_category = large_city
			}
		}
	}

	LIT_litangyia_industrial_complex_build_2 = {
		icon = generic_research

		visible = {
			has_country_flag = LIT_litangyia_industrial_complex_build_1_flag
		}

		available = {
			has_country_flag = LIT_litangyia_construct_1_flag
		}

		fire_only_once = yes
		cost = 60
		days_remove = 100

		ai_will_do = {
			factor = 90
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision LIT_litangyia_industrial_complex_build_2"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove LIT_litangyia_industrial_complex_build_2"
			set_country_flag = LIT_litangyia_industrial_complex_build_2_flag
			188 = {
				add_extra_state_shared_building_slots = 2
				add_building_construction = {
					type = industrial_complex
					level = 2
					instant_build = yes
				}
			}
		}
	}

	LIT_litangyia_arms_factory_build_2 = {
		icon = generic_research

		visible = {
			has_country_flag = LIT_litangyia_arms_factory_build_1_flag
		}

		available = {
			has_country_flag = LIT_litangyia_construct_1_flag
		}

		fire_only_once = yes
		cost = 60
		days_remove = 100

		ai_will_do = {
			factor = 90
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision LIT_litangyia_arms_factory_build_2"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove LIT_litangyia_arms_factory_build_2"
			set_country_flag = LIT_litangyia_arms_factory_build_2_flag
			188 = {
				add_extra_state_shared_building_slots = 2
				add_building_construction = {
					type = arms_factory
					level = 2
					instant_build = yes
				}
			}
		}
	}

	LIT_litangyia_sanxia_power_transfer = {
		icon = generic_research

		visible = {
			has_country_flag = LIT_litangyia_construct_1_flag
		}

		available = {
			has_completed_focus = LIT_sanxia_hydropower
		}

		fire_only_once = yes
		cost = 80
		days_remove = 120

		ai_will_do = {
			factor = 90
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision LIT_litangyia_sanxia_power_transfer"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove LIT_litangyia_sanxia_power_transfer"
			set_country_flag = LIT_litangyia_sanxia_power_transfer_flag
			every_state = {
				limit = { OR = { state = 239 state = 253 state = 263 state = 277 state = 243 state = 261} }
				set_country_flag = LIT_litangyia_sanxia_power_transfer_flag
				add_building_construction = {
					type = infrastructure
					level = 1
					instant_build = yes
				}
			}
			add_ideas = LIT_sanxia_power_idea
		}
	}

	LIT_litangyia_construct_2 = {
		icon = generic_research

		visible = {
			has_completed_focus = LIT_litang_urbanization
			has_country_flag = LIT_litangyia_construct_1_flag
		}

		available = {
			AND = {
				has_country_flag = LIT_litangyia_sanxia_power_transfer_flag
				has_country_flag = LIT_litangyia_industrial_complex_build_2_flag
				has_country_flag = LIT_litangyia_arms_factory_build_2_flag
			}
		}

		fire_only_once = yes
		cost = 40
		days_remove = 30

		ai_will_do = {
			factor = 90
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision LIT_litangyia_construct_2"

		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove LIT_litangyia_construct_2"
			set_country_flag = LIT_litangyia_construct_2_flag
			188 = {
				set_state_category = megalopolis
			}
		}
	}

	LIT_litangyia_construct_final = {
		icon = generic_research

		visible = {
			has_completed_focus = LIT_litangyia_millennium
		}

		available = {
			has_country_flag = LIT_litangyia_construct_2_flag
		}

		fire_only_once = yes
		cost = 45
		days_remove = 210

		ai_will_do = {
			factor = 90
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision LIT_litangyia_construct_final"
			country_event = {
				id = litang.1000
				days = 1
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove LIT_litangyia_construct_final"
			set_country_flag = LIT_litangyia_construct_final_flag
		}
	}

}

LIT_break_iron_curtain_category = {
	LIT_break_iron_curtain_decision = {
		allowed = {
			tag = LIT
		}
		icon = generic_research

		visible = {
			has_completed_focus = LIT_break_iron_curtain
			country_exists = LIN
			NOT = {
				has_war_with = LIN
			}
			NOT = {
				is_ally_with = LIN
			}
		}

		available = {
			has_war = no
			has_idea = GEN_iron_curtain_idea
			NOT = { has_country_flag = LIT_prepare_conquer_flag }
		}

		fire_only_once = no
		cost = 50
		days_remove = 20

		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				has_war = yes
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision LIT_break_iron_curtain_decision"
			set_country_flag = LIT_prepare_conquer_flag
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove LIT_break_iron_curtain_decision"
			create_wargoal = {
				type = annex_everything
				target = LIN
			}
			remove_ideas = GEN_iron_curtain_idea
			clr_country_flag = LIT_prepare_conquer_flag
		}
	}

	LIT_conquer_neimenggu_decision = {
		allowed = {
			tag = LIT
		}
		icon = generic_research

		visible = {
			has_completed_focus = LIT_break_iron_curtain
			country_exists = NMG
			NOT = {
				has_war_with = NMG
			}
			NOT = {
				is_ally_with = NMG
			}
		}

		available = {
			has_war = no
			NOT = { has_country_flag = LIT_prepare_conquer_flag }
			is_neighbor_of = NMG
		}

		fire_only_once = no
		cost = 20
		days_remove = 15

		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				country_exists = LIN
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision LIT_conquer_neimenggu_decision"
			set_country_flag = LIT_prepare_conquer_flag
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove LIT_conquer_neimenggu_decision"
			create_wargoal = {
				type = annex_everything
				target = NMG
			}
			remove_ideas = GEN_iron_curtain_idea
			clr_country_flag = LIT_prepare_conquer_flag
		}
	}

	LIT_conquer_neimenggueast_decision = {
		allowed = {
			tag = LIT
		}
		icon = generic_research

		visible = {
			has_completed_focus = LIT_break_iron_curtain
			country_exists = NME
			NOT = {
				has_war_with = NME
			}
			NOT = {
				is_ally_with = NME
			}
		}

		available = {
			has_war = no
			NOT = { has_country_flag = LIT_prepare_conquer_flag }
			is_neighbor_of = NME
		}

		fire_only_once = no
		cost = 20
		days_remove = 15

		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				country_exists = LIN
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision LIT_conquer_neimenggueast_decision"
			set_country_flag = LIT_prepare_conquer_flag
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove LIT_conquer_neimenggueast_decision"
			create_wargoal = {
				type = annex_everything
				target = NME
			}
			remove_ideas = GEN_iron_curtain_idea
			clr_country_flag = LIT_prepare_conquer_flag
		}
	}
}

LIT_final_fight_category = {
	LIT_conquer_xinjiang_decision = {
		allowed = {
			tag = LIT
		}
		icon = generic_research

		visible = {
			OR = {
				has_completed_focus = LIT_recover_northwest
				has_completed_focus = LIT_north_boundary
			}

			country_exists = XIJ
			NOT = {
				has_war_with = XIJ
			}
			NOT = {
				is_ally_with = XIJ
			}
		}

		available = {
			has_war = no
			NOT = { has_country_flag = LIT_prepare_conquer_flag }
			is_neighbor_of = XIJ
		}

		fire_only_once = no
		cost = 30
		days_remove = 15

		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				country_exists = LIN
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision LIT_conquer_xinjiang_decision"
			set_country_flag = LIT_prepare_conquer_flag
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove LIT_conquer_xinjiang_decision"
			create_wargoal = {
				type = annex_everything
				target = XIJ
			}
			clr_country_flag = LIT_prepare_conquer_flag
		}
	}

	LIT_conquer_qinghai_decision = {
		allowed = {
			tag = LIT
		}
		icon = generic_research

		visible = {
			OR = {
				has_completed_focus = LIT_recover_northwest
				has_completed_focus = LIT_north_boundary
			}
			country_exists = QIH
			NOT = {
				has_war_with = QIH
			}
			NOT = {
				is_ally_with = QIH
			}
		}

		available = {
			has_war = no
			NOT = { has_country_flag = LIT_prepare_conquer_flag }
			is_neighbor_of = QIH
		}

		fire_only_once = no
		cost = 30
		days_remove = 15

		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				country_exists = LIN
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision LIT_conquer_qinghai_decision"
			set_country_flag = LIT_prepare_conquer_flag
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove LIT_conquer_qinghai_decision"
			create_wargoal = {
				type = annex_everything
				target = QIH
			}
			clr_country_flag = LIT_prepare_conquer_flag
		}
	}

	LIT_conquer_gansu_decision = {
		allowed = {
			tag = LIT
		}
		icon = generic_research

		visible = {
			OR = {
				has_completed_focus = LIT_recover_northwest
				has_completed_focus = LIT_north_boundary
			}
			country_exists = GAS
			NOT = {
				has_war_with = GAS
			}
			NOT = {
				is_ally_with = GAS
			}
		}

		available = {
			has_war = no
			NOT = { has_country_flag = LIT_prepare_conquer_flag }
			is_neighbor_of = GAS
		}

		fire_only_once = no
		cost = 20
		days_remove = 15

		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				country_exists = LIN
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision LIT_conquer_gansu_decision"
			set_country_flag = LIT_prepare_conquer_flag
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove LIT_conquer_gansu_decision"
			create_wargoal = {
				type = annex_everything
				target = GAS
			}
			clr_country_flag = LIT_prepare_conquer_flag
		}
	}

	LIT_conquer_ningxia_decision = {
		allowed = {
			tag = LIT
		}
		icon = generic_research

		visible = {
			OR = {
				has_completed_focus = LIT_recover_northwest
				has_completed_focus = LIT_north_boundary
			}
			country_exists = NIX
			NOT = {
				has_war_with = NIX
			}
			NOT = {
				is_ally_with = NIX
			}
		}

		available = {
			has_war = no
			NOT = { has_country_flag = LIT_prepare_conquer_flag }
			is_neighbor_of = NIX
		}

		fire_only_once = no
		cost = 10
		days_remove = 15

		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				country_exists = LIN
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision LIT_conquer_ningxia_decision"
			set_country_flag = LIT_prepare_conquer_flag
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove LIT_conquer_ningxia_decision"
			create_wargoal = {
				type = annex_everything
				target = NIX
			}
			clr_country_flag = LIT_prepare_conquer_flag
		}
	}

	LIT_conquer_yunnan_decision = {
		allowed = {
			tag = LIT
		}
		icon = generic_research

		visible = {
			OR = {
				has_completed_focus = LIT_southwest_invade
				has_completed_focus = LIT_yungui_puppet
			}
			country_exists = YUN
			NOT = {
				has_war_with = YUN
			}
			NOT = {
				is_ally_with = YUN
			}
		}

		available = {
			has_war = no
			NOT = { has_country_flag = LIT_prepare_conquer_flag }
			is_neighbor_of = YUN
		}

		fire_only_once = no
		cost = 30
		days_remove = 15

		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				country_exists = LIN
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision LIT_conquer_yunnan_decision"
			set_country_flag = LIT_prepare_conquer_flag
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove LIT_conquer_yunnan_decision"
			create_wargoal = {
				type = annex_everything
				target = YUN
			}
			clr_country_flag = LIT_prepare_conquer_flag
		}
	}

	LIT_conquer_guizhou_decision = {
		allowed = {
			tag = LIT
		}
		icon = generic_research

		visible = {
			OR = {
				has_completed_focus = LIT_southwest_invade
				has_completed_focus = LIT_yungui_puppet
			}
			country_exists = GUZ
			NOT = {
				has_war_with = GUZ
			}
			NOT = {
				is_ally_with = GUZ
			}
		}

		available = {
			has_war = no
			NOT = { has_country_flag = LIT_prepare_conquer_flag }
			is_neighbor_of = GUZ
		}

		fire_only_once = no
		cost = 20
		days_remove = 15

		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				country_exists = LIN
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision LIT_conquer_guizhou_decision"
			set_country_flag = LIT_prepare_conquer_flag
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove LIT_conquer_guizhou_decision"
			create_wargoal = {
				type = annex_everything
				target = GUZ
			}
			clr_country_flag = LIT_prepare_conquer_flag
		}
	}

	LIT_conquer_shaanxi_decision = {
		allowed = {
			tag = LIT
		}
		icon = generic_research

		visible = {
			OR = {
				has_completed_focus = LIT_attack_changan
				has_completed_focus = LIT_east_boundary
			}
			country_exists = SAX
			NOT = {
				has_war_with = SAX
			}
			NOT = {
				is_ally_with = SAX
			}
		}

		available = {
			has_war = no
			NOT = { has_country_flag = LIT_prepare_conquer_flag }
			is_neighbor_of = SAX
		}

		fire_only_once = no
		cost = 20
		days_remove = 15

		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				country_exists = LIN
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision LIT_conquer_shaanxi_decision"
			set_country_flag = LIT_prepare_conquer_flag
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove LIT_conquer_shaanxi_decision"
			create_wargoal = {
				type = annex_everything
				target = SAX
			}
			clr_country_flag = LIT_prepare_conquer_flag
		}
	}

	LIT_conquer_jiangsu_decision = {
		allowed = {
			tag = LIT
		}
		icon = generic_research

		visible = {
			OR = {
				has_completed_focus = LIT_end_jiangsu_chaos
				has_completed_focus = LIT_zhongyuan_puppet
				has_completed_focus = LIT_jiangnan_puppet
			}
			country_exists = JIS
			NOT = {
				has_war_with = JIS
			}
			NOT = {
				is_ally_with = JIS
			}
		}

		available = {
			has_war = no
			NOT = { has_country_flag = LIT_prepare_conquer_flag }
			is_neighbor_of = JIS
		}

		fire_only_once = no
		cost = 30
		days_remove = 15

		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				country_exists = LIN
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision LIT_conquer_jiangsu_decision"
			set_country_flag = LIT_prepare_conquer_flag
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove LIT_conquer_jiangsu_decision"
			create_wargoal = {
				type = annex_everything
				target = JIS
			}
			clr_country_flag = LIT_prepare_conquer_flag
		}
	}

	LIT_conquer_shanghai_decision = {
		allowed = {
			tag = LIT
		}
		icon = generic_research

		visible = {
			OR = {
				has_completed_focus = LIT_end_jiangsu_chaos
				has_completed_focus = LIT_jiangnan_puppet
			}
			country_exists = SHH
			NOT = {
				has_war_with = SHH
			}
			NOT = {
				is_ally_with = SHH
			}
		}

		available = {
			has_war = no
			NOT = { has_country_flag = LIT_prepare_conquer_flag }
			is_neighbor_of = SHH
		}

		fire_only_once = no
		cost = 20
		days_remove = 15

		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				country_exists = LIN
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision LIT_conquer_shanghai_decision"
			set_country_flag = LIT_prepare_conquer_flag
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove LIT_conquer_shanghai_decision"
			create_wargoal = {
				type = annex_everything
				target = SHH
			}
			clr_country_flag = LIT_prepare_conquer_flag
		}
	}

	LIT_conquer_anhui_decision = {
		allowed = {
			tag = LIT
		}
		icon = generic_research

		visible = {
			OR = {
				has_completed_focus = LIT_dingzhen_visit_jiangnan
				has_completed_focus = LIT_zhongyuan_puppet
				has_completed_focus = LIT_jiangnan_puppet
			}
			country_exists = ANH
			NOT = {
				has_war_with = ANH
			}
			NOT = {
				is_ally_with = ANH
			}
		}

		available = {
			has_war = no
			NOT = { has_country_flag = LIT_prepare_conquer_flag }
			is_neighbor_of = ANH
		}

		fire_only_once = no
		cost = 30
		days_remove = 15

		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				country_exists = LIN
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision LIT_conquer_anhui_decision"
			set_country_flag = LIT_prepare_conquer_flag
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove LIT_conquer_anhui_decision"
			create_wargoal = {
				type = annex_everything
				target = ANH
			}
			clr_country_flag = LIT_prepare_conquer_flag
		}
	}

	LIT_conquer_zhejiang_decision = {
		allowed = {
			tag = LIT
		}
		icon = generic_research

		visible = {
			OR = {
				has_completed_focus = LIT_dingzhen_visit_jiangnan
				has_completed_focus = LIT_jiangnan_puppet
			}
			country_exists = ZHJ
			NOT = {
				has_war_with = ZHJ
			}
			NOT = {
				is_ally_with = ZHJ
			}
		}

		available = {
			has_war = no
			NOT = { has_country_flag = LIT_prepare_conquer_flag }
			is_neighbor_of = ZHJ
		}

		fire_only_once = no
		cost = 30
		days_remove = 15

		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				country_exists = LIN
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision LIT_conquer_zhejiang_decision"
			set_country_flag = LIT_prepare_conquer_flag
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove LIT_conquer_zhejiang_decision"
			create_wargoal = {
				type = annex_everything
				target = ZHJ
			}
			clr_country_flag = LIT_prepare_conquer_flag
		}
	}

	LIT_conquer_hubei_decision = {
		allowed = {
			tag = LIT
		}
		icon = generic_research

		visible = {
			OR = {
				has_completed_focus = LIT_end_southeast_mutual_insurance
				has_completed_focus = LIT_lianghu_puppet
			}
			country_exists = HUB
			NOT = {
				has_war_with = HUB
			}
			NOT = {
				is_ally_with = HUB
			}
		}

		available = {
			has_war = no
			NOT = { has_country_flag = LIT_prepare_conquer_flag }
			is_neighbor_of = HUB
		}

		fire_only_once = no
		cost = 20
		days_remove = 15

		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				country_exists = LIN
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision LIT_conquer_hubei_decision"
			set_country_flag = LIT_prepare_conquer_flag
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove LIT_conquer_hubei_decision"
			create_wargoal = {
				type = annex_everything
				target = HUB
			}
			clr_country_flag = LIT_prepare_conquer_flag
		}
	}

	LIT_conquer_hunan_decision = {
		allowed = {
			tag = LIT
		}
		icon = generic_research

		visible = {
			OR = {
				has_completed_focus = LIT_end_southeast_mutual_insurance
				has_completed_focus = LIT_lianghu_puppet
			}
			country_exists = HUN
			NOT = {
				has_war_with = HUN
			}
			NOT = {
				is_ally_with = HUN
			}
		}

		available = {
			has_war = no
			NOT = { has_country_flag = LIT_prepare_conquer_flag }
			is_neighbor_of = HUN
		}

		fire_only_once = no
		cost = 20
		days_remove = 15

		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				country_exists = LIN
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision LIT_conquer_hunan_decision"
			set_country_flag = LIT_prepare_conquer_flag
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove LIT_conquer_hunan_decision"
			create_wargoal = {
				type = annex_everything
				target = HUN
			}
			clr_country_flag = LIT_prepare_conquer_flag
		}
	}

	LIT_conquer_jiangxi_decision = {
		allowed = {
			tag = LIT
		}
		icon = generic_research

		visible = {
			OR = {
				has_completed_focus = LIT_end_southeast_mutual_insurance
				has_completed_focus = LIT_jiangnan_puppet
			}
			country_exists = JIX
			NOT = {
				has_war_with = JIX
			}
			NOT = {
				is_ally_with = JIX
			}
		}

		available = {
			has_war = no
			NOT = { has_country_flag = LIT_prepare_conquer_flag }
			is_neighbor_of = JIX
		}

		fire_only_once = no
		cost = 20
		days_remove = 15

		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				country_exists = LIN
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision LIT_conquer_jiangxi_decision"
			set_country_flag = LIT_prepare_conquer_flag
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove LIT_conquer_jiangxi_decision"
			create_wargoal = {
				type = annex_everything
				target = JIX
			}
			clr_country_flag = LIT_prepare_conquer_flag
		}
	}

	LIT_conquer_fujian_decision = {
		allowed = {
			tag = LIT
		}
		icon = generic_research

		visible = {
			OR = {
				has_completed_focus = LIT_end_southeast_mutual_insurance
				has_completed_focus = LIT_jiangnan_puppet
			}
			country_exists = FUJ
			NOT = {
				has_war_with = FUJ
			}
			NOT = {
				is_ally_with = FUJ
			}
		}

		available = {
			has_war = no
			NOT = { has_country_flag = LIT_prepare_conquer_flag }
			is_neighbor_of = FUJ
		}

		fire_only_once = no
		cost = 20
		days_remove = 15

		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				country_exists = LIN
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision LIT_conquer_fujian_decision"
			set_country_flag = LIT_prepare_conquer_flag
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove LIT_conquer_fujian_decision"
			create_wargoal = {
				type = annex_everything
				target = FUJ
			}
			clr_country_flag = LIT_prepare_conquer_flag
		}
	}

	LIT_conquer_guangxi_decision = {
		allowed = {
			tag = LIT
		}
		icon = generic_research

		visible = {
			OR = {
				has_completed_focus = LIT_to_south_sea
				has_completed_focus = LIT_nanhai_puppet
			}
			country_exists = GUX
			NOT = {
				has_war_with = GUX
			}
			NOT = {
				is_ally_with = GUX
			}
		}

		available = {
			has_war = no
			NOT = { has_country_flag = LIT_prepare_conquer_flag }
			is_neighbor_of = GUX
		}

		fire_only_once = no
		cost = 20
		days_remove = 15

		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				country_exists = LIN
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision LIT_conquer_guangxi_decision"
			set_country_flag = LIT_prepare_conquer_flag
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove LIT_conquer_guangxi_decision"
			create_wargoal = {
				type = annex_everything
				target = GUX
			}
			clr_country_flag = LIT_prepare_conquer_flag
		}
	}

	LIT_conquer_guangdong_decision = {
		allowed = {
			tag = LIT
		}
		icon = generic_research

		visible = {
			OR = {
				has_completed_focus = LIT_to_south_sea
				has_completed_focus = LIT_nanhai_puppet
			}
			country_exists = GUD
			NOT = {
				has_war_with = GUD
			}
			NOT = {
				is_ally_with = GUD
			}
		}

		available = {
			has_war = no
			NOT = { has_country_flag = LIT_prepare_conquer_flag }
			is_neighbor_of = GUD
		}

		fire_only_once = no
		cost = 30
		days_remove = 15

		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				country_exists = LIN
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision LIT_conquer_guangdong_decision"
			set_country_flag = LIT_prepare_conquer_flag
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove LIT_conquer_guangdong_decision"
			create_wargoal = {
				type = annex_everything
				target = GUD
			}
			clr_country_flag = LIT_prepare_conquer_flag
		}
	}

	LIT_conquer_hainan_decision = {
		allowed = {
			tag = LIT
		}
		icon = generic_research

		visible = {
			OR = {
				has_completed_focus = LIT_to_south_sea
				has_completed_focus = LIT_nanhai_puppet
			}
			country_exists = HAN
			NOT = {
				has_war_with = HAN
			}
			NOT = {
				is_ally_with = HAN
			}
		}

		available = {
			has_war = no
			NOT = { has_country_flag = LIT_prepare_conquer_flag }
			is_neighbor_of = HAN
		}

		fire_only_once = no
		cost = 10
		days_remove = 15

		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				country_exists = LIN
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision LIT_conquer_hainan_decision"
			set_country_flag = LIT_prepare_conquer_flag
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove LIT_conquer_hainan_decision"
			create_wargoal = {
				type = annex_everything
				target = HAN
			}
			clr_country_flag = LIT_prepare_conquer_flag
		}
	}

	LIT_conquer_hongkong_decision = {
		allowed = {
			tag = LIT
		}
		icon = generic_research

		visible = {
			OR = {
				has_completed_focus = LIT_to_south_sea
				has_completed_focus = LIT_nanhai_puppet
			}
			country_exists = HOK
			NOT = {
				has_war_with = HOK
			}
			NOT = {
				is_ally_with = HOK
			}
		}

		available = {
			has_war = no
			NOT = { has_country_flag = LIT_prepare_conquer_flag }
			is_neighbor_of = HOK
		}

		fire_only_once = no
		cost = 10
		days_remove = 15

		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				country_exists = LIN
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision LIT_conquer_hongkong_decision"
			set_country_flag = LIT_prepare_conquer_flag
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove LIT_conquer_hongkong_decision"
			create_wargoal = {
				type = annex_everything
				target = HOK
			}
			clr_country_flag = LIT_prepare_conquer_flag
		}
	}

	LIT_conquer_macau_decision = {
		allowed = {
			tag = LIT
		}
		icon = generic_research

		visible = {
			OR = {
				has_completed_focus = LIT_to_south_sea
				has_completed_focus = LIT_nanhai_puppet
			}
			country_exists = MAC
			NOT = {
				has_war_with = MAC
			}
			NOT = {
				is_ally_with = MAC
			}
		}

		available = {
			has_war = no
			NOT = { has_country_flag = LIT_prepare_conquer_flag }
			is_neighbor_of = MAC
		}

		fire_only_once = no
		cost = 10
		days_remove = 15

		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				country_exists = LIN
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision LIT_conquer_macau_decision"
			set_country_flag = LIT_prepare_conquer_flag
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove LIT_conquer_macau_decision"
			create_wargoal = {
				type = annex_everything
				target = MAC
			}
			clr_country_flag = LIT_prepare_conquer_flag
		}
	}

	LIT_conquer_taiwan_decision = {
		allowed = {
			tag = LIT
		}
		icon = generic_research

		visible = {
			OR = {
				has_completed_focus = LIT_unfinished_business
				has_completed_focus = LIT_nanhai_puppet
			}
			country_exists = TAW
			NOT = {
				has_war_with = TAW
			}
			NOT = {
				is_ally_with = TAW
			}
		}

		available = {
			has_war = no
			NOT = { has_country_flag = LIT_prepare_conquer_flag }
			is_neighbor_of = TAW
		}

		fire_only_once = no
		cost = 30
		days_remove = 15

		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				country_exists = LIN
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision LIT_conquer_taiwan_decision"
			set_country_flag = LIT_prepare_conquer_flag
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove LIT_conquer_taiwan_decision"
			create_wargoal = {
				type = annex_everything
				target = TAW
			}
			clr_country_flag = LIT_prepare_conquer_flag
		}
	}

}

LIT_core_china_category = {
	LIT_core_tibet_decision = {
		allowed = {
			tag = LIT
		}
		icon = generic_research

		visible = {
			has_completed_focus = LIT_integrate_tibet
			country_exists = TIB
			is_ally_with = TIB
		}

		available = {
		}

		fire_only_once = no
		cost = 50
		days_remove = 50

		ai_will_do = {
			factor = 90
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision LIT_core_tibet_decision"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove LIT_core_tibet_decision"
			annex_country = {
				target = TIB
				transfer_troops = yes
			}
			every_state = {
				limit = {
					is_core_of = TIB
				}
				add_core_of = LIT
			}
		}
	}
}