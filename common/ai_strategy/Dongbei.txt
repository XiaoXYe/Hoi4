NMG_defend_LIN = {
    allowed = {
        original_tag = NMG
    }
    enable = {
        country_exists = LIN
		owns_state = 37  # ³à·å
		LIN = { is_subject = no }
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = prepare_for_war
        id = LIN
        value = 100
    }
}

LIN_invade_NMG = {
	allowed = {
		original_tag = LIN
	}
	enable = {
		is_subject = no
		NMG = {
			is_subject = no
			OR = {
				is_in_faction = yes
				owns_state = 37  # ³à·å
			}
		}
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = prepare_for_war
		id = NMG
		value = 100
	}
}

JIL_invade_NMG = {
	allowed = {
		tag = JIL
	}
	enable = {
		is_subject = no
		NMG = {
			is_subject = no
			OR = {
				is_in_faction = yes
				owns_state = 19 # ÐË°²
				owns_state = 30 # Í¨ÁÉ
			}
		}
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = prepare_for_war
		id = NMG
		value = 100
	}
}

NMG_defend_JIL = {
	allowed = {
		tag = NMG
	}
	enable = {
		is_subject = no
		OR = {
			owns_state = 19 # ÐË°²
			owns_state = 30 # Í¨ÁÉ
		}
		JIL = { is_subject = no }
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = prepare_for_war
		id = JIL
		value = 100
	}
}

HLJ_invade_NMG = {
	allowed = {
		tag = HLJ
	}
	enable = {
		is_subject = no
		NMG = {
			is_subject = no
			OR = {
				is_in_faction = yes
				owns_state = 9  # ºôÂ×±´¶û
			}
		}
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = prepare_for_war
		id = NMG
		value = 100
	}
}

NMG_defend_HLJ = {
	allowed = {
		tag = NMG
	}
	enable = {
		is_subject = no
		owns_state = 9  # ºôÂ×±´¶û
		HLJ = { is_subject = no }
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = prepare_for_war
		id = HLJ
		value = 100
	}
}

LIN_invade_HLJ = {
	allowed = {
		original_tag = LIN
	}
	enable = {
		is_subject = no
		HLJ = {
			is_subject = no
			OR = {
				is_in_faction = not
			}
		}
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = declare_war
		id = HLJ
		value = 100
	}
}

LIN_invade_BEJ = {
	allowed = {
		original_tag = LIN
	}
	enable = {
		is_subject = no
		BEJ = {
			is_subject = no
		}
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = prepare_for_war
		id = BEJ
		value = 100
	}
}